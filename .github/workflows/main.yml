name: Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production   # 🔹 GitHub will require approval
    steps:
      - name: Deploy
        run: echo "Deploying..."
